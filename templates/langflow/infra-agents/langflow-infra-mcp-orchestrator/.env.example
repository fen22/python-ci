# Langflow Infra MCP Orchestrator — Variables
# Configurar en los nodos del UI de Langflow, no como .env del sistema

# LLM API Key (Anthropic Claude)
LLM_API_KEY=sk-ant-PLACEHOLDER-DO-NOT-USE

# GitHub Fine-grained token (read-only)
GITHUB_TOKEN=github_pat_PLACEHOLDER-DO-NOT-USE

# API Key para Lambda proxy de AWS (si tienes API Gateway con key)
INFRA_API_KEY=PLACEHOLDER-DO-NOT-USE
